services:
  # One-shot: ensure shared volumes are owned by mcpwner (UID 1000) and writable.
  volume-init:
    image: alpine:3.19
    user: "0:0"
    volumes:
      - workspaces:/workspaces
      - logs:/var/log/mcpwner
    command:
      - sh
      - -ec
      - |
        mkdir -p /workspaces/.metadata /var/log/mcpwner
        chown -R 1000:1000 /workspaces /var/log/mcpwner
        chmod 1777 /workspaces
    restart: "no"

  mcpwner:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: mcpwner-server
    env_file:
      - docker/common/python.env
    user: "1000:1000"
    stdin_open: true
    tty: true
    read_only: true
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    ports:
      - "13370:13370"
    volumes:
      - workspaces:/workspaces
      - logs:/var/log/mcpwner
      - ./config/config.yaml:/app/config/config.yaml:ro
      - ./src:/app/src:ro
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
      - /app/cache:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
      - mcpwner-recon
    environment:
      - MCP_TRANSPORT=${MCP_TRANSPORT:-stdio}
    depends_on:
      volume-init:
        condition: service_completed_successfully
      codeql:
        condition: service_healthy
      linguist:
        condition: service_healthy
      bandit:
        condition: service_healthy
      gosec:
        condition: service_healthy
      brakeman:
        condition: service_healthy
      pmd:
        condition: service_healthy
      psalm:
        condition: service_healthy
      nodejsscan:
        condition: service_healthy
      joern:
        condition: service_healthy
      yasa:
        condition: service_healthy
      opengrep:
        condition: service_healthy
      gitleaks:
        condition: service_healthy
      trufflehog:
        condition: service_healthy
      whispers:
        condition: service_healthy
      detect-secrets:
        condition: service_healthy
      hawk-scanner:
        condition: service_healthy
      osv-scanner:
        condition: service_healthy
      grype:
        condition: service_healthy
      syft:
        condition: service_healthy
      retirejs:
        condition: service_healthy
      subfinder:
        condition: service_healthy
      amass:
        condition: service_healthy
      masscan:
        condition: service_healthy
      nmap:
        condition: service_healthy
      ffuf:
        condition: service_healthy
      bbot:
        condition: service_healthy
      httpx:
        condition: service_healthy
      katana:
        condition: service_healthy
      gau:
        condition: service_healthy
      arjun:
        condition: service_healthy
      wafw00f:
        condition: service_healthy
      kiterunner:
        condition: service_healthy
      wiremock:
        condition: service_healthy
      mitmproxy:
        condition: service_healthy
      fuzzer:
        condition: service_healthy
      chromium:
        condition: service_healthy
      checkov:
        condition: service_healthy
      kics:
        condition: service_healthy
      terrascan:
        condition: service_healthy
      tfsec:
        condition: service_healthy
      hadolint:
        condition: service_healthy

  codeql:
    # Using digest for reproducibility (equivalent to latest as of 2026-01-23)
    # To update: docker pull mcr.microsoft.com/cstsectools/codeql-container:latest
    # and get new digest with: docker inspect mcr.microsoft.com/cstsectools/codeql-container:latest
    build:
      context: .
      dockerfile: docker/codeql/Dockerfile
    container_name: codeql-executor
    env_file:
      - docker/common/python.env
    user: "1000:1000"
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - mcpwner-internal
      - mcpwner-recon   # internet: autobuild needs to resolve build dependencies
    environment:
      # With internet available, let CodeQL install dependencies for richer analysis 
      - CODEQL_EXTRACTOR_JAVASCRIPT_OPTION_INSTALL_DEPENDENCIES=true
      - LGTM_INDEX_INSTALL_DEPENDENCIES=true
    read_only: false  # autobuild writes build artifacts / dependency caches
    deploy:
      resources:
        limits:
          memory: 8g
          cpus: "4.0"
        reservations:
          memory: 1g
    volumes:
      - workspaces:/workspaces
      # Local directory mounts can be added dynamically:
      # - /path/to/local/dir:/workspaces/{workspace_id}/source:ro
    tmpfs:
      - /tmp:noexec,nosuid,size=2g
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8080/health\")"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 45s

  linguist:
    build:
      context: .
      dockerfile: docker/linguist/Dockerfile
    container_name: linguist-detector
    env_file:
      - docker/common/python.env
    # Run as UID 1000 to match the owner of cloned workspaces, otherwise
    # libgit2/Rugged rejects the repo ("not owned by current user").
    user: "1000:1000"
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces:ro
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8081/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  semgrep:

    build:
      context: .
      dockerfile: docker/sast/semgrep/Dockerfile
    container_name: semgrep-scanner
    env_file:
      - docker/common/python.env
    environment:
      # Internet available: use the curated Semgrep Registry ruleset ('auto')
      # instead of the bundled offline copy. 'auto' requires metrics to be
      # enabled (it queries the registry), so override the image's off default.
      - SEMGREP_DEFAULT_CONFIG=auto
      - SEMGREP_SEND_METRICS=auto
      # This container runs as root, but GitHub clones are owned by UID 1000 (the
      # server that cloned them). Git rejects such repos ("dubious ownership"), so
      # `git ls-files` returns nothing — and semgrep discovers its scan targets via
      # git, meaning it would scan 0 files and report 0 findings on every cloned
      # repo regardless of language. Tell git to trust any repo path.
      - GIT_CONFIG_COUNT=1
      - GIT_CONFIG_KEY_0=safe.directory
      - "GIT_CONFIG_VALUE_0=*"
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
        reservations:
          memory: 512m
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
      - mcpwner-recon   # internet: fetch rules from the Semgrep Registry
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8082/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  bandit:

    build:
      context: .
      dockerfile: docker/sast/bandit/Dockerfile
    container_name: bandit-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8083/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  gosec:

    build:
      context: .
      dockerfile: docker/sast/gosec/Dockerfile
    container_name: gosec-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8084/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  brakeman:

    build:
      context: .
      dockerfile: docker/sast/brakeman/Dockerfile
    container_name: brakeman-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8085/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  pmd:

    build:
      context: .
      dockerfile: docker/sast/pmd/Dockerfile
    container_name: pmd-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8086/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  psalm:

    build:
      context: .
      dockerfile: docker/sast/psalm/Dockerfile
    container_name: psalm-scanner
    env_file:
      - docker/common/python.env
    user: "1000:1000"
    read_only: true
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8087/health\")"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

  nodejsscan:

    build:
      context: .
      dockerfile: docker/sast/nodejsscan/Dockerfile
    container_name: nodejsscan-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8088/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  joern:

    build:
      context: .
      dockerfile: docker/sast/joern/Dockerfile
    container_name: joern-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 4g
          cpus: "2.0"
        reservations:
          memory: 512m
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:exec,size=2g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8089/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

  yasa:

    build:
      context: .
      dockerfile: docker/sast/yasa/Dockerfile
    container_name: yasa-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=2g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8095/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  opengrep:

    build:
      context: .
      dockerfile: docker/sast/opengrep/Dockerfile
    container_name: opengrep-scanner
    env_file:
      - docker/common/python.env
    environment:
      # Internet available: use the curated registry ruleset ('auto').
      - OPENGREP_DEFAULT_CONFIG=auto
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=2g
    networks:
      - mcpwner-internal
      - mcpwner-recon   # internet: fetch rules from the registry
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8096/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  gitleaks:

    build:
      context: .
      dockerfile: docker/secrets/gitleaks/Dockerfile
    container_name: gitleaks-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8090/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  trufflehog:

    build:
      context: .
      dockerfile: docker/secrets/trufflehog/Dockerfile
    container_name: trufflehog-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8091/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  whispers:

    build:
      context: .
      dockerfile: docker/secrets/whispers/Dockerfile
    container_name: whispers-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8092/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  detect-secrets:

    build:
      context: .
      dockerfile: docker/secrets/detect_secrets/Dockerfile
    container_name: detect-secrets-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8093/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  hawk-scanner:

    build:
      context: .
      dockerfile: docker/secrets/hawk_scanner/Dockerfile
    container_name: hawk-scanner-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8094/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  osv-scanner:

    build:
      context: .
      dockerfile: docker/sca/osv-scanner/Dockerfile
    container_name: osv-scanner-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8100/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  grype:

    build:
      context: .
      dockerfile: docker/sca/grype/Dockerfile
    container_name: grype-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8101/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  syft:

    build:
      context: .
      dockerfile: docker/sca/syft/Dockerfile
    container_name: syft-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8102/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  retirejs:

    build:
      context: .
      dockerfile: docker/sca/retirejs/Dockerfile
    container_name: retirejs-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8104/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  subfinder:

    build:
      context: .
      dockerfile: docker/reconnaissance/subfinder/Dockerfile
    container_name: subfinder-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8110/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  amass:

    build:
      context: .
      dockerfile: docker/reconnaissance/amass/Dockerfile
    container_name: amass-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8111/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  masscan:

    build:
      context: .
      dockerfile: docker/reconnaissance/masscan/Dockerfile
    container_name: masscan-scanner
    env_file:
      - docker/common/python.env
    user: "0:0"  # Run as root (required for raw socket access)
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8117/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3

  nmap:

    build:
      context: .
      dockerfile: docker/reconnaissance/nmap/Dockerfile
    container_name: nmap-scanner
    env_file:
      - docker/common/python.env
    user: "0:0"  # Run as root (required for some scan types)
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8116/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  ffuf:

    build:
      context: .
      dockerfile: docker/reconnaissance/ffuf/Dockerfile
    container_name: ffuf-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8114/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  bbot:

    build:
      context: .
      dockerfile: docker/reconnaissance/bbot/Dockerfile
    container_name: bbot-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
        reservations:
          memory: 512m
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8118/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  httpx:

    build:
      context: .
      dockerfile: docker/reconnaissance/httpx/Dockerfile
    container_name: httpx-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8112/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  katana:

    build:
      context: .
      dockerfile: docker/reconnaissance/katana/Dockerfile
    container_name: katana-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8113/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  gau:

    build:
      context: .
      dockerfile: docker/reconnaissance/gau/Dockerfile
    container_name: gau-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8115/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  arjun:

    build:
      context: .
      dockerfile: docker/reconnaissance/arjun/Dockerfile
    container_name: arjun-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8119/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  wafw00f:

    build:
      context: .
      dockerfile: docker/reconnaissance/wafw00f/Dockerfile
    container_name: wafw00f-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8120/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  kiterunner:

    build:
      context: .
      dockerfile: docker/reconnaissance/kiterunner/Dockerfile
    container_name: kiterunner-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-recon
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8121/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  wiremock:

    build:
      context: .
      dockerfile: docker/utilities/wiremock/Dockerfile
    container_name: wiremock-service
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8130/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 25s

  mitmproxy:

    build:
      context: .
      dockerfile: docker/utilities/mitmproxy/Dockerfile
    container_name: mitmproxy-service
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-recon   # internet: intercepts traffic to external targets
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8131/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  fuzzer:

    build:
      context: .
      dockerfile: docker/utilities/fuzzer/Dockerfile
    container_name: fuzzer-service
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-recon   # internet: fuzzes external targets
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8132/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 10s

  chromium:

    build:
      context: .
      dockerfile: docker/utilities/chromium/Dockerfile
    container_name: chromium-service
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - SYS_ADMIN   # required for Chromium sandbox
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
        reservations:
          memory: 512m
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:exec,size=1g   # Chromium needs exec in /tmp
      - /dev/shm:exec,size=512m
    networks:
      - mcpwner-recon   # internet: browses external websites
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8133/health\")"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 30s

  # --- Infrastructure & IaC Security ---
  # IaC scanners analyze static infrastructure definitions, so they run on the
  # internet-free internal network like the other static-analysis tools.

  checkov:

    build:
      context: .
      dockerfile: docker/iac/checkov/Dockerfile
    container_name: checkov-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8140/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

  kics:

    build:
      context: .
      dockerfile: docker/iac/kics/Dockerfile
    container_name: kics-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=1g
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8141/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  terrascan:

    build:
      context: .
      dockerfile: docker/iac/terrascan/Dockerfile
    container_name: terrascan-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
      - mcpwner-recon   # internet: Terrascan resolves Terraform provider schemas from registry.terraform.io
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8142/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  tfsec:

    build:
      context: .
      dockerfile: docker/iac/tfsec/Dockerfile
    container_name: tfsec-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8143/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

  hadolint:

    build:
      context: .
      dockerfile: docker/iac/hadolint/Dockerfile
    container_name: hadolint-scanner
    env_file:
      - docker/common/python.env
    read_only: false
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    volumes:
      - workspaces:/workspaces
    tmpfs:
      - /tmp:noexec,nosuid,size=512m
    networks:
      - mcpwner-internal
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen(\"http://localhost:8144/health\")"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 15s

volumes:
  workspaces:
    driver: local
  logs:
    driver: local

networks:
  mcpwner-internal:
    driver: bridge
    internal: true          # No internet for SAST/SCA/secrets/codeql/linguist

  mcpwner-recon:
    driver: bridge
    internal: false         # Recon tools need internet
