## Summary

<!-- What does this PR change, in 1-3 bullets? -->

-

## Problem

<!-- What was broken or missing? Link the issue if there is one (e.g. Closes #123). -->

## Verification

<!-- How did you confirm this works? Commands run, output, builds that now pass, tests. -->

- [ ] `ruff format` / `ruff check --fix` run on changed Python files
- [ ] Tests pass (`pytest tests/unit`) where applicable
- [ ] Affected containers build and come up healthy (`docker compose build <svc>` / `docker compose ps`)
