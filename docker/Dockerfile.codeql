FROM mcr.microsoft.com/cstsectools/codeql-container@sha256:0fd52101e30652e0998be85f6e09ac59534c6f40cebb670acd77b78869c45bc2

# Run as root for installation
USER root

# Install Python, FastAPI/uvicorn, newer Node.js (v18+) for CodeQL JS extractor, and latest Golang
RUN apt-get update && \
    apt-get install -y python3 python3-pip curl gnupg && \
    # Install Node.js
    mkdir -p /etc/apt/keyrings && \
    curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg && \
    echo "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_20.x nodistro main" | tee /etc/apt/sources.list.d/nodesource.list && \
    apt-get update && \
    apt-get install -y nodejs && \
    # Install latest Golang
    curl -OL https://go.dev/dl/go1.23.2.linux-amd64.tar.gz && \
    tar -C /usr/local -xzf go1.23.2.linux-amd64.tar.gz && \
    rm go1.23.2.linux-amd64.tar.gz && \
    # Cleanup
    rm -rf /var/lib/apt/lists/*

ENV PATH=$PATH:/usr/local/go/bin

WORKDIR /service

# Copy service files
COPY config/codeql_requirements.txt .
RUN pip3 install --no-cache-dir -r codeql_requirements.txt

COPY docker/codeql_service.py .

# Expose service port
EXPOSE 8080

# Override the base image's entrypoint to avoid startup script issues
ENTRYPOINT []

# Run the FastAPI service with uvicorn
CMD ["python3", "-m", "uvicorn", "codeql_service:app", "--host", "0.0.0.0", "--port", "8080"]
