# Shared Python runtime settings for all MCPwner service containers.
# Referenced once from docker-compose.yaml via env_file (do not duplicate in Dockerfiles).
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1
